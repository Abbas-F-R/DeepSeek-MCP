node_modules/
dist/
logs/
.env
*.log

# Agent memory is per-checkout state, not source.
.agent/

# A local .mcp.json for developing against this checkout (machine-specific, never committed).
.mcp.json
